#pragma once

#include "player.h"
#include "info.h"

Info runTicTacToe(const Player& firstPlayer, const Player& secondPlayer);

Info runBattleship(const Player& firstPlayer, const Player& secondPlayer);

Info runHangman(const Player& firstPlayer, const Player& secondPlayer);

Info runGuess(const Player& firstPlayer, const Player& secondPlayer);